Question: {{question}}

User's time range: {{userTimeRangeStart}} to {{userTimeRangeEnd}}

Repositories:
{{{repoContext}}}

Recent updates (title, date, significance):
{{{updateContext}}}

Select the relevant repositories and narrow the time range to answer this question. Return JSON with repoIds (array of globalRepoId strings) and dateRange (start and end ISO date strings within the user's bounds).
